{
    "name": "repos",
    "devDependencies": {
        "@types/node": "^14.14.6",
        "@types/mocha": "^8.0.3",
        "tslint": "^6.1.3"
    },
    "scripts": {
        "test": "mocha -r ts-node/register tests/**/*.ts",
        "test:dev": "mocha --require ts-node/register --watch --watch-files *.ts, tests/**/*.ts",
        "lint": "tslint -p ."
    },
    "dependencies": {
        "@pulumi/github": "^2.2.0",
        "mocha": "^8.2.0",
        "ts-node": "^9.0.0"
    }
}
